{
  "name": "taylors-tech",
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "sst dev",
    "dev:notes": "cd packages/notes && sst dev npm run dev",
    "deploy": "sst deploy",
    "test": "echo \"Error: no test specified\" && exit 1",
    "db": "sst shell drizzle-kit",
    "db:studio": "sst shell drizzle-kit studio"
  },
  "devDependencies": {
    "@tsconfig/node20": "^20.1.4",
    "typescript": "5.3.3"
  },
  "dependencies": {
    "drizzle-kit": "^0.21.4",
    "drizzle-orm": "^0.30.10",
    "sst": "^3.0.4"
  }
}
